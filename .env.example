# Environment Variables for CarrilloApps
# Copy this file to .env.local for local development
# For Vercel deployment, configure these in Project Settings

# =============================================================================
# PUBLIC VARIABLES (Exposed to the browser)
# These variables are prefixed with NEXT_PUBLIC_ and will be inlined at build time
# =============================================================================

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://carrillo.app
NEXT_PUBLIC_BASE_URL=https://carrillo.app

# Disqus Configuration (for blog comments)
# Get your shortname from https://disqus.com/admin/
NEXT_PUBLIC_DISQUS_SHORTNAME=juniorcarrillo

# WhatsApp lead destination
# All contact / scheduling forms open a WhatsApp chat with this number using a
# pre-filled message template. Digits only (country code + number); any
# formatting is stripped automatically. Defaults to the test number if unset.
NEXT_PUBLIC_WHATSAPP_PHONE=573003328389

# =============================================================================
# ANALYTICS & TRACKING
# =============================================================================

# Google Analytics 4 (GA4)
# Get your Measurement ID from https://analytics.google.com/
# Format: G-XXXXXXXXXX
# Required for: User behavior analytics, traffic sources, conversions
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Microsoft Clarity
# Get your Project ID from https://clarity.microsoft.com/
# Format: abc123def
# Required for: Heatmaps, session recordings, user behavior insights
NEXT_PUBLIC_CLARITY_PROJECT_ID=

# Google Tag Manager (optional - if you prefer GTM over direct GA4)
# Format: GTM-XXXXXXX
# NEXT_PUBLIC_GOOGLE_TAG_MANAGER_ID=

# =============================================================================
# PRIVATE VARIABLES (Server-side only)
# These variables are only available on the server
# =============================================================================

# Disqus API (optional - for advanced features like comment count)
# Get from https://disqus.com/api/applications/
DISQUS_API_KEY=your_disqus_api_key_here
DISQUS_API_SECRET=your_disqus_api_secret_here
DISQUS_ACCESS_TOKEN=your_disqus_access_token_here

# Database (uncomment if needed in the future)
# DATABASE_URL=postgresql://username:password@localhost:5432/carrilloapps
# MONGODB_URI=mongodb://localhost:27017/carrilloapps

# Email Service (uncomment if needed for contact forms)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Mailchimp (newsletter subscriptions)
# The newsletter form is fully wired — set these three to go live. While they
# are empty the form shows a "coming soon" state and disables the button.
#   - API key format: <key>-<serverPrefix> (e.g. abc123...-us21)
#   - Audience/List ID: Mailchimp > Audience > Settings > "Audience ID"
#   - Server prefix: the suffix after the dash in the API key (e.g. us21)
# MAILCHIMP_API_KEY=your-mailchimp-api-key
# MAILCHIMP_AUDIENCE_ID=your-audience-id
# MAILCHIMP_SERVER_PREFIX=us21

# API Keys (uncomment if needed)
# GITHUB_TOKEN=your-github-token
# GITLAB_TOKEN=your-gitlab-token

# =============================================================================
# VERCEL SPECIFIC VARIABLES
# These are automatically provided by Vercel - DO NOT SET MANUALLY
# =============================================================================

# VERCEL=1 (automatically set by Vercel)
# VERCEL_URL (automatically set by Vercel)
# VERCEL_ENV (automatically set by Vercel: development, preview, production)
# VERCEL_REGION (automatically set by Vercel)